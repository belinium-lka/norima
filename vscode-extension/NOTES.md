VS Code extension notes

- Language id: `norima`
- File extension: `.norm`
- Logo (SVG): `../logos.norm/norm file logo.svg`

The `package.json` in this folder registers the `.norm` extension. Place client/server code under `client/` and `server/`.
