# compiler

Frontend and middle-end for Norima: lexer, parser, AST, typechecker, IR, and optimizations.

Planned: Tree-sitter grammar, type system spec, and phased compiler (interpreter -> bytecode VM -> LLVM).
